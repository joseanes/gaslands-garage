import { VehicleReport, Team } from './model';

export function checkWeaponSlots(team: Team, report: VehicleReport[]) {
  // e.g. max 2 weapon slots on a Buggy
}

export function checkSponsorPerks(team: Team, report: VehicleReport[]) {
  // e.g. vehicles must only take perks from sponsor OR universal
}


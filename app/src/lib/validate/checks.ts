// app/src/lib/validate/checks.ts
import type { Validation } from './model';

/** Placeholder rule set that always passes */
export function runAllChecks(report: Validation) {
  // push errors into report.errors or vehicleReports[i].errors here
}

